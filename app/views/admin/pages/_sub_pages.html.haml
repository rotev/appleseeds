.list_with_image
	- pages.each do |page|
		%article.clearfix
			.image
				- if page.image
					= link_to image_tag(page.image.url(:small)), admin_page_path(page)

			= link_to page.title, admin_page_path(page)
			%p= page.subtitle
			.content= page.text.html_safe
